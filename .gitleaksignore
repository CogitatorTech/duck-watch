# Findings that have been looked at and are not secrets. Each line is one
# finding's fingerprint, so a different secret in the same file still fails.

# `TOKEN_ENCRYPTION_KEY=""` in the example env file. The value is empty on
# purpose, and the generic-api-key rule runs past the end of the line and
# reports the next setting's name as the secret. There is no key here; the
# real one is generated by `make setup` into a gitignored .env.
39ef662f4572b4a53bb5b0c31e3f147da13ec4d4:backend/.env.example:generic-api-key:13
